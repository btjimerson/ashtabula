apiVersion: v1
kind: Service
metadata:
  name: frontend-lb
  namespace: shopping-cart
  labels:
    app: frontend
    service: frontend-lb
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    name: http
  selector:
    app: frontend